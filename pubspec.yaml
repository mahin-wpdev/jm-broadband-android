name: jm_broadband_app
description: Unified server-selectable ISP mobile app foundation.
publish_to: none
version: 1.0.4+5
environment:
  sdk: '>=3.3.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  flutter_secure_storage: ^9.2.4
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/branding/logo.png
    - assets/branding/icon.png
